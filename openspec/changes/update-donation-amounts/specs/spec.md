## No New Capabilities

This change modifies an existing implementation detail (preset amount values) within the `DonationWidget` component. No spec-level requirements change — the widget accepts the same props, renders the same payment methods, and follows the same behavior. Only the numeric preset values differ.

No ADDED, MODIFIED, REMOVED, or RENAMED requirements.
